import { Head, router } from '@inertiajs/vue3';
import { computed, defineComponent, ref } from 'vue';
import AdminShell from '../../components/AdminShell';
import DashboardChart from '../../components/DashboardChart';

const palette = ['#07559e', '#00aeef', '#38bdf8', '#7dd3fc', '#1e40af', '#0f766e', '#f59e0b', '#64748b'];
const statusTones = ['bg-amber-100 text-amber-700', 'bg-sky-100 text-[#07559e]', 'bg-violet-100 text-violet-700', 'bg-emerald-100 text-emerald-700', 'bg-rose-100 text-rose-700'];

export default defineComponent({
    name: 'AdminDashboard',
    components: { AdminShell, DashboardChart, Head },
    props: { filters: { type: Object, required: true }, series: { type: Array, required: true }, serviceBreakdown: { type: Array, required: true }, stats: { type: Object, required: true }, statusBreakdown: { type: Array, required: true } },
    setup(props) {
        const month = ref(props.filters.month);
        const changeMonth = () => router.get('/dashboard', { month: month.value }, { preserveScroll: true, preserveState: true, replace: true });
        const moveMonth = (offset) => {
            const [year, selectedMonth] = month.value.split('-').map(Number);
            const date = new Date(year, selectedMonth - 1 + offset, 1);
            month.value = `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, '0')}`;
            changeMonth();
        };
        const baseOptions = { plugins: { legend: { labels: { boxWidth: 12, color: '#334155', font: { family: 'Poppins' } } }, tooltip: { titleFont: { family: 'Poppins' }, bodyFont: { family: 'Poppins' } } } };
        const trendData = computed(() => ({ labels: props.series.map((point) => point.date.slice(5)), datasets: [{ label: 'Requests', data: props.series.map((point) => point.total), borderColor: '#00aeef', backgroundColor: 'rgba(0, 174, 239, 0.15)', fill: true, tension: 0.35, pointBackgroundColor: '#07559e', pointRadius: 4 }] }));
        const trendOptions = { ...baseOptions, scales: { x: { grid: { display: false }, ticks: { color: '#64748b', font: { family: 'Poppins' } } }, y: { beginAtZero: true, ticks: { precision: 0, color: '#64748b', font: { family: 'Poppins' } }, grid: { color: '#e2e8f0' } } } };
        const statusData = computed(() => ({ labels: props.statusBreakdown.map((item) => item.label), datasets: [{ data: props.statusBreakdown.map((item) => item.total), backgroundColor: palette, borderColor: '#ffffff', borderWidth: 3 }] }));
        const statusOptions = { ...baseOptions, plugins: { ...baseOptions.plugins, legend: { position: 'bottom', labels: { boxWidth: 12, color: '#334155', font: { family: 'Poppins' } } } } };
        const serviceData = computed(() => ({ labels: props.serviceBreakdown.map((item) => item.label), datasets: [{ label: 'Requests', data: props.serviceBreakdown.map((item) => item.total), backgroundColor: '#07559e', borderRadius: 6, borderSkipped: false }] }));
        const serviceOptions = { ...baseOptions, indexAxis: 'y', plugins: { ...baseOptions.plugins, legend: { display: false } }, scales: { x: { beginAtZero: true, ticks: { precision: 0, color: '#64748b', font: { family: 'Poppins' } }, grid: { color: '#e2e8f0' } }, y: { ticks: { color: '#334155', font: { family: 'Poppins', size: 11 } }, grid: { display: false } } } };
        const statusCards = computed(() => props.statusBreakdown.map((status, index) => ({ ...status, tone: statusTones[index] })));
        return { changeMonth, month, moveMonth, serviceData, serviceOptions, statusCards, statusData, statusOptions, trendData, trendOptions };
    },
    template: `<Head title="Dashboard" /><AdminShell active="dashboard" title="Dashboard"><section class="flex flex-wrap items-end justify-between gap-4 border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><div><p class="text-xs font-bold uppercase tracking-[0.18em] text-[#07559e]">Dashboard overview</p><h2 class="mt-1 text-2xl font-bold text-slate-900">Monthly request statistics</h2><p class="mt-1 text-slate-600">Review submission activity and service demand for the selected month.</p></div><div><p class="mb-2 text-sm font-semibold text-slate-700">Month</p><div class="inline-flex items-center rounded-lg border border-slate-300 bg-white shadow-sm"><button type="button" class="flex h-12 w-12 items-center justify-center text-[#07559e] transition hover:bg-sky-50" aria-label="Previous month" @click="moveMonth(-1)"><i class="fa-solid fa-chevron-left" aria-hidden="true"></i></button><input v-model="month" type="month" class="h-12 border-x border-slate-200 bg-transparent px-3 text-sm font-semibold text-slate-700 outline-none focus:bg-sky-50" aria-label="Select month" @change="changeMonth" /><button type="button" class="flex h-12 w-12 items-center justify-center text-[#07559e] transition hover:bg-sky-50" aria-label="Next month" @click="moveMonth(1)"><i class="fa-solid fa-chevron-right" aria-hidden="true"></i></button></div></div></section><section class="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><article v-for="item in [{label:'All requests',value:stats.total,icon:'fa-clipboard-list',tone:'text-[#07559e] bg-sky-100'},{label:'Walk-ins',value:stats.walkIns,icon:'fa-person-walking',tone:'text-cyan-700 bg-cyan-100'},{label:'Appointments',value:stats.appointments,icon:'fa-calendar-check',tone:'text-violet-700 bg-violet-100'},{label:'Pending',value:stats.pending,icon:'fa-clock',tone:'text-amber-700 bg-amber-100'}]" :key="item.label" class="border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-start justify-between gap-4"><div><p class="text-sm font-semibold text-slate-500">{{ item.label }}</p><p class="mt-3 text-3xl font-bold text-slate-900">{{ item.value }}</p></div><span class="flex h-11 w-11 items-center justify-center rounded-xl" :class="item.tone"><i class="fa-solid" :class="item.icon" aria-hidden="true"></i></span></div></article></section><section class="mt-6 border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><div><p class="text-xs font-bold uppercase tracking-[0.18em] text-[#07559e]">Request status</p><h2 class="mt-1 text-xl font-bold text-slate-900">Status totals</h2><p class="mt-1 text-sm text-slate-600">All workflow statuses for the selected month.</p></div><div class="mt-5 grid gap-3 sm:grid-cols-5"><article v-for="status in statusCards" :key="status.label" class="border border-slate-200 p-4"><span class="inline-flex rounded-full px-3 py-1 text-xs font-bold uppercase" :class="status.tone">{{ status.label }}</span><p class="mt-4 text-3xl font-bold text-slate-900">{{ status.total }}</p><p class="mt-1 text-sm text-slate-500">requests</p></article></div></section><section class="mt-6 border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><div><p class="text-xs font-bold uppercase tracking-[0.18em] text-[#07559e]">Request activity</p><h2 class="mt-1 text-xl font-bold text-slate-900">Daily request volume</h2><p class="mt-1 text-sm text-slate-600">Submitted requests during the selected month.</p></div><div class="mt-6"><DashboardChart chart-type="line" :chart-data="trendData" :options="trendOptions" /></div></section><section class="mt-6 grid gap-6 xl:grid-cols-2"><article class="border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><div><p class="text-xs font-bold uppercase tracking-[0.18em] text-[#07559e]">Request status</p><h2 class="mt-1 text-xl font-bold text-slate-900">Requests by status</h2><p class="mt-1 text-sm text-slate-600">Distribution across each workflow status.</p></div><div class="mt-6"><DashboardChart chart-type="pie" :chart-data="statusData" :options="statusOptions" /></div></article><article class="border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><div><p class="text-xs font-bold uppercase tracking-[0.18em] text-[#07559e]">Service demand</p><h2 class="mt-1 text-xl font-bold text-slate-900">Requests by service</h2><p class="mt-1 text-sm text-slate-600">Compare demand across PTRI service offerings.</p></div><div class="mt-6"><DashboardChart chart-type="bar" :chart-data="serviceData" :options="serviceOptions" /></div></article></section></AdminShell>`,
});
